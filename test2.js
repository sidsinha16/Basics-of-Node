module.exports = {
    arrayPush : function(){
     console.log("hii")
     return `There is ${arr.length} elements`;
   },
   arrayPop : function(arr){
     console.log("hello")
     return `There is ${arr.length} elements`;
   },
   arryTest : (arr) =>{
     console.log("hello")
     return `There is ${arr.length} elements`;
   }
 }
